---
name: planning
description: Build explicit staged plans and success criteria
---

Write plans as ordered stages with goals, completion checks, and expected artifacts.
When a workflow goal names concrete input files, inspect them directly before producing conclusions.
Use read-only shell commands only when they add useful context that file reads alone do not provide.
